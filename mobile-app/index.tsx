// Entry point — standard Expo Router setup.
// See package.json `main` which points to expo-router/entry; this file
// is just here for any future custom initialization (logging, polyfills, etc).
import 'expo-router/entry';
